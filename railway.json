{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "nixPkgs": [
            "nodejs",
            "npm",
            "python39Full",
            "python39Packages.pip",
            "python39Packages.virtualenv"
          ]
        },
        "install": {
          "cmds": [
            "cd bot && npm install",
            "cd api && python3.10 -m venv venv",
            "cd api && . venv/bin/activate && pip install --no-cache-dir --disable-pip-version-check --no-warn-script-location -r requirements.txt"
          ]
        }
      }
    }
  },
  "deploy": {
    "startCommand": "sh -c 'cd api && . venv/bin/activate && python app.py & cd bot && node index.js'"
  }
}
